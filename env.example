# ── Environment ──────────────────────────────────────────────────────────
ENVIRONMENT=development          # development | production

# ── API ──────────────────────────────────────────────────────────────────
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true                  # hot-reload in dev (disable in production)
LOG_LEVEL=INFO

# ── JWT ──────────────────────────────────────────────────────────────────
# IMPORTANT: generate a strong secret for production:
#   openssl rand -hex 32
JWT_SECRET=dev-secret-change-me-in-production
JWT_EXPIRY_DAYS=7

# ── Database ─────────────────────────────────────────────────────────────
# SQLite file path (created automatically on first run)
DB_PATH=data/tennis_court_finder.db

# ── SMTP (optional — leave blank for console-only mode) ──────────────────
# When these are empty, notification & OTP emails are printed to the console.
SMTP_ENABLED=false                # auto | true | false — set to false to print emails to console
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@tenniscourtfinder.local
SMTP_USE_TLS=true

# ── Notifier ─────────────────────────────────────────────────────────────
# How often to check for slot status transitions (seconds)
NOTIFIER_INTERVAL=60
# Min seconds between two emails for the same subscription
NOTIFIER_COOLDOWN=300
